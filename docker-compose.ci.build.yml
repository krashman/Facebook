version: '2'

services:
  ci-build:
    image: microsoft/aspnetcore-build:1.0.3-msbuild-preview
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore && npm run build-production && dotnet publish -c Release -o ./bin/Release/PublishOutput"
